{# Base template for AGENTS.md - Package provides this #}
---
version: "{{ sdk_version }}"
platform: "{{ platform }}"
generated: "{{ generated_at }}"
---

{% block header %}
# HtmlGraph Agent Documentation
{% endblock %}

{% block introduction %}
## Introduction

HtmlGraph is a local-first observability and coordination platform for AI-assisted development. Document-backed work items (HTML artifacts) with SQLite-indexed runtime state, append-only JSONL event log, live FastAPI/HTMX dashboard, and multi-agent session tracking. Single-process, local-first stack -- no external database server required.

**Origin philosophy**: "HTML is All You Need" -- work items are stored as HTML files for durability, human readability, and git compatibility.
{% endblock %}

{% block quick_start %}
## Quick Start

```python
from htmlgraph import SDK

# Initialize SDK for your agent
sdk = SDK(agent="{{ platform }}")

# Create a feature
feature = sdk.features.create("Add authentication") \
    .set_priority("high") \
    .add_steps(["Setup OAuth", "Add JWT", "Test login"]) \
    .save()

# Track progress
sdk.features.get(feature.id).complete_step(0).save()
```
{% endblock %}

{% block core_concepts %}
{% include "_sections/core_concepts.md.j2" %}
{% endblock %}

{% block sdk_reference %}
{% include "_sections/sdk_basics.md.j2" %}
{% endblock %}

{% block cli_reference %}
{% include "_sections/cli_reference.md.j2" %}
{% endblock %}

{% block custom_workflows %}
{# Users can override this block in .htmlgraph/docs/templates/agents.md.j2 #}
{% if custom_workflows %}
## Custom Workflows

{{ custom_workflows }}
{% endif %}
{% endblock %}

{% block deployment %}
## Deployment

For deployment instructions, see the deployment guide:

```bash
# Build and publish package
./scripts/deploy-all.sh 0.21.0 --no-confirm

# Documentation changes only
./scripts/deploy-all.sh --docs-only
```
{% endblock %}

{% block footer %}
---
*Generated by HtmlGraph v{{ sdk_version }} on {{ generated_at }}*
{% endblock %}
